name: Test Workflow Trigger

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Test step
        run: echo "Workflow triggered successfully for commit ${{ github.sha }}"